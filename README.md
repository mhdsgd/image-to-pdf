# 图片合并PDF工具

一款简单高效的图片合并PDF桌面软件，支持批量导入图片、拖拽排序、自定义页面布局和压缩质量，提供实时预览功能。

## 功能特性

- 支持JPG、PNG、WebP格式图片导入
- 支持从ZIP、7z压缩包直接导入图片
- 支持批量导入，一次最多处理1000张图片
- 默认按文件名排序，支持手动拖拽排序
- 实时PDF预览，支持缩放和翻页
- 自定义页面大小（A4、Letter）
- 自定义页面方向（纵向/横向）
- 自定义页边距
- 多种压缩质量选项（原画质/高质量/中等/低质量）
- 自定义输出文件名
- 浅色/深色主题切换
- PDF生成进度条显示

## 安装方法

### 方式一：直接运行（推荐）

1. 下载仓库中的 `dist/图片合并PDF工具.exe`
2. 双击运行即可

### 方式二：从源码运行

1. 克隆项目
```bash
git clone https://github.com/mhdsgd/image-to-pdf.git
cd image-to-pdf
```

2. 安装依赖
```bash
pip install -r requirements.txt
```

3. 运行程序
```bash
python main.py
```

### 方式三：从源码打包（需要Python环境）

**前提条件：** 需要安装 Python 3.7+ 并添加到系统 PATH。

1. 克隆项目
```bash
git clone https://github.com/mhdsgd/image-to-pdf.git
cd image-to-pdf
```

2. 运行打包脚本
```bash
build.bat
```

打包完成后，可执行文件位于 `dist/图片合并PDF工具.exe`。

## 使用说明

### 导入图片

1. 点击"导入图片"按钮
2. 选择要导入的图片文件（支持多选）
3. 图片将显示在左侧列表中

### 导入压缩包

1. 点击"导入压缩包"按钮
2. 选择ZIP或7z格式的压缩包文件
3. 压缩包中的图片将自动解压并显示在列表中

### 排序图片

- 默认按文件名排序
- 拖拽图片可调整顺序
- 右键菜单可进行删除、上移、下移操作

### 预览PDF

- 右侧面板实时预览PDF效果
- 使用"上一页"/"下一页"按钮翻页
- 使用"放大"/"缩小"按钮调整缩放

### 生成PDF

1. 点击"设置"按钮调整页面参数
2. 点击"生成PDF"按钮
3. 选择保存位置和文件名
4. 等待生成完成

### 设置选项

- **页面大小**：A4、Letter
- **页面方向**：纵向、横向
- **页边距**：0-200点
- **压缩质量**：原画质、高质量、中等、低质量
- **界面主题**：浅色、深色

## 常见问题

### Q: 支持哪些图片格式？
A: 支持JPG、JPEG、PNG、WebP格式。

### Q: 支持哪些压缩包格式？
A: 支持ZIP和7z格式的压缩包。

### Q: 一次最多能处理多少张图片？
A: 一次最多处理1000张图片。

### Q: 生成的PDF文件太大怎么办？
A: 可以在设置中选择"中等"或"低质量"压缩选项。

### Q: 如何切换主题？
A: 点击"设置"按钮，在主题设置中选择"浅色"或"深色"。

## 技术支持

如有问题或建议，请提交Issue或联系开发者。

## 许可证

## 更新日志

- **2026-05-09**: PDF生成性能优化（消除临时文件磁盘I/O）、预览缩放改用PIL先缩后转（翻页更流畅）、新增PDF生成进度条、文件选择器改用原生对话框
- **2025-05-05**: 初始版本，支持图片导入/压缩包导入/排序/预览/PDF生成/主题切换

## 许可证

MIT License
